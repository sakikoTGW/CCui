/**
 * 构建脚本占位 — 原版使用 Bun bundle + feature() DCE。
 * 完整构建需配置 MACRO.VERSION、feature flags 等编译期常量。
 */
console.log('Build pipeline: configure bun build with entry src/entrypoints/cli.tsx')
console.log('See docs/ARCHITECTURE.md § 构建系统')
